{
	"version": "2.0.0",
	"tasks": [
        {
            "type": "dub",
            "run": false,
            "cwd": "${workspaceFolder}",
            "compiler": "$current",
            "archType": "$current",
            "buildType": "$current",
            "configuration": "$current",
            "problemMatcher": [
                "$dmd"
            ],
            "group": "build",
            "label": "dub: Build craftascii",
            "detail": "dub build --compiler=dmd -a=x86_64 -b=debug -c=application"
        }
    ]
}